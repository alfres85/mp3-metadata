{
  "name": "mp3-auto-cover",
  "version": "1.0.0",
  "description": "Tool that scans MP3 files, reads ID3 tags, fetches missing covers using MusicBrainz/CoverArtArchive/DuckDuckGo, and embeds them.",
  "main": "dist/index.js",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "nodemon --watch src --watch index.ts --exec node --loader ts-node/esm index.ts",
    "recognize": "nodemon --watch src --watch index.ts --exec node --loader ts-node/esm index.ts -- --recognize",
    "openai-recon": "nodemon --watch src --watch index.ts --exec node --loader ts-node/esm index.ts -- --openai-recon",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "gui": "cmd /c start index.html"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "fpcalc": "^1.3.0",
    "node-id3": "^0.2.1",
    "node-shazam": "^1.2.7",
    "recursive-readdir": "^2.2.3",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "@types/recursive-readdir": "^2.2.1",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
